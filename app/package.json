{
  "name": "solar-pv-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:prep": "vite build && node scripts/cap-prep.mjs",
    "cap:sync": "npm run cap:prep && cap sync",
    "cap:android": "npm run cap:sync && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.7",
    "@capacitor/core": "^7.6.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.7",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.3.5"
  }
}
